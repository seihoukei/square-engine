export default class Square2DSceneElement {
    data = {}

    constructor(scene) {
        this.scene = scene
        this.renderer = scene.renderer
        this.init()
    }

    init() {

    }

    render(now) {

    }
}